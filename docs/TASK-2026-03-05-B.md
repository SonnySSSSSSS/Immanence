# TASK-2026-03-05-B: Side Panel Image Fill + Vertical Metric Reflow

**Created:** 2026-03-05  
**Recommended Executor:** VS Code agent (Copilot) or Codex  
**Scope:** HomeHub left side panel layout only (single atomic UI fix)  
**Flags:** `IMPLEMENT UNCOMMITTED, VERIFY FIRST`

---

## Goal

1. Make the lower image tile fill the entire available container space in the left HomeHub side panel.  
2. Move the right metric number (`Active`) below the top metric (`Sessions`) so the numbers are vertically stacked.  
3. Keep overall placement visually balanced.

---

## Problem Statement

In the left HomeHub side panel:

- The image tile appears undersized relative to available panel space.
- The two top metrics are laid out side-by-side, but requirement is vertical stacking.
- Current spacing/alignment feels unbalanced.

---

## Single Hypothesis

The imbalance is caused by `sidePanelTileStatsRowStyle` forcing a 2-column grid and `sidePanelTileImageStyle` using a fixed small height instead of consuming remaining vertical space.

---

## Single Atomic Change

Adjust only the left panel layout styles in `HomeHub.jsx` so:

- metrics become a single-column vertical stack,
- image tile flexes to fill remaining container height,
- spacing and centering are tuned for balanced composition.

No cross-component refactor.

---

## Reuse-First Plan (Mandatory)

1. `src/components/HomeHub.jsx`

- Reuse existing style objects and panel markup as-is.
- Extend/adjust `sidePanelTileStatsRowStyle`, `sidePanelTileMetricCellStyle`, and `sidePanelTileImageStyle` only.

No new component, no new store, no new utility.

---

## Spec Lint Gate (Mandatory)

- Single hypothesis: yes  
- Single atomic change: yes  
- Exact file allowlist: yes  
- Explicit no-sequencing rule: yes  
- Clear screenshot pass/fail signal: yes  
- Explicit stop gate: yes

If any item is missing, reject implementation.

---

## Stateful UI Debug Gate (Mandatory)

1. **Exact render surface**

- `src/components/HomeHub.jsx`
- Left panel block labeled `/* LEFT PANEL - Sessions + Active Days */`

1. **Exact upstream state source**

- `hubTiles?.sessions_total`
- `hubTiles?.days_active`

1. **Concrete failing runtime case**

- In the left panel, confirm both numbers are currently on one row and image does not fill available lower area.

1. **Visual vs logic isolation**

- Temporarily set static mock values for sessions/days (for example `11` and `6`) to prove layout behavior is independent of data values.
- Remove temporary forcing before final state.

1. **Exact implementation predicate**

- Left panel metrics render in one column with `Sessions` on top and `Active` directly below.
- Image block below metrics consumes remaining available panel content height.

If any proof is missing, stop.

---

## ALLOWLIST (Files to Modify)

1. `src/components/HomeHub.jsx`

---

## DENYLIST (Files NOT to Modify)

1. `src/components/DailyPracticeCard.jsx`
2. `src/components/CompactStatsCard.jsx`
3. `src/index.css`
4. `src/App.jsx`
5. `src/state/uiStore.js`

---

## Constraints

1. Left panel only; right panel layout remains unchanged.
2. Do not change metric semantics, labels, or data source.
3. Do not add new assets.
4. Keep existing panel collapse/expand behavior working.
5. Maintain mobile and desktop readability.
6. Do not modify files outside ALLOWLIST.
7. **No-sequencing rule:** this task is a single visual layout fix only.

---

## Verification

### Manual UI Verification

1. Run app:

- `npm run dev`

1. Open HomeHub and inspect left side panel:

- Confirm top metric is `Sessions`.
- Confirm second metric (`Active`) is directly below it (not side-by-side).
- Confirm bottom image fills the remaining content area of the panel.
- Confirm spacing looks centered and balanced vertically.

1. Responsive check:

- Desktop width and narrow width (mobile emulation).
- Ensure no overlap, clipping, or collapsed text.

### Optional Guardrail

1. `npx eslint src/components/HomeHub.jsx`

### Screenshot Success/Fail Signal (Mandatory)

1. **Fail screenshot signal:** left panel shows `Sessions` and `Active` on the same row OR image remains a short strip not filling panel remainder.
2. **Pass screenshot signal:** left panel shows vertical metric stack (`Sessions` above `Active`) and image occupies the full remaining lower area with balanced spacing.

---

## Commit Message

`fix(homehub): stack left-panel metrics vertically and make image fill panel space`

---

## Stop Gate (Mandatory)

After implementation commit, halt and wait for explicit human pass/fail before any follow-up edits.
