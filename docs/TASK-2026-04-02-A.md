# TASK-2026-04-02-A

## Goal

Change the existing circled header control into a dedicated Home button. The control must perform one action only: return the app to Home Hub. Remove any other navigational behavior or semantics this control currently carries.

## Files to Modify (ALLOWLIST)

- `src/App.jsx`
- `src/App.css`

## Files NOT to Modify (DENYLIST)

- `src/state/navigationStore.js`
- `src/components/NavigationSection.jsx`
- `src/components/HomeHub.jsx`
- `src/components/CurriculumHub.jsx`
- `src/tutorials/pageTutorialDefinitions.js`
- `src/context/ThemeContext.jsx`

## Constraints

- Single hypothesis: the circled header control in the sticky app header is the owner surface and can be converted into a Home-only control by changing its render semantics and click behavior in `src/App.jsx`, with optional presentation cleanup in `src/App.css`.
- Single atomic change only: convert the existing header control into a Home-only button.
- Reuse the existing header control; do not add a second header navigation button, route, store, or component.
- Keep the action boundary in the header layer only.
- The button must resolve to Home Hub by setting the app section to Home only.
- Remove any non-Home navigational semantics from this control:
- no section cycling
- no contextual destination switching
- no multi-target navigation behavior
- Preserve surrounding header controls and version display.
- Keep any style changes scoped to existing header-button classes in `src/App.css`.
- No sequencing; perform only the listed atomic change.
- Implement uncommitted, verify first.
- No commit without human approval.

## Runtime Proof Gate

1. Exact failing behavior: the circled header control is not behaving as a dedicated Home button.
2. Runtime source of truth: `activeSection` and `setActiveSection` in `src/App.jsx`.
3. Concrete failing case:
- Input/state: user is inside any non-home section where `activeSection !== null`.
- Actual output: the control presents or retains broader navigation semantics instead of a pure Home action.
- Expected output: activating the control always sends the user to Home Hub only.
4. Correction boundary: header rendering and click predicate only.

## Stateful UI Debug Gate

1. Exact render surface: sticky top header control cluster in `src/App.jsx`.
2. Exact upstream state source: `activeSection`, derived `isHub`, and the control's click handler.
3. Proof step: while on a non-home section, force the control to call only `setActiveSection(null)` and confirm it returns to Home Hub; remove any temporary force before finalizing.
4. Exact implementation predicate: if the control is shown, its only navigation effect is `setActiveSection(null)`.

## Verification Steps

1. Run `npm run build`.
2. Start from any non-home section.
3. Click the edited header control.
4. Confirm the app returns to Home Hub.
5. Confirm the control no longer implies or performs any navigation behavior other than Home.

## Screenshot Signal

- Success: the header shows the edited control as a Home button, clicking it lands on Home Hub, and no alternate navigation affordance remains on that control.
- Fail: the control still behaves like contextual navigation, multi-target navigation, or anything other than Home-only.

## Stop Gate

Stop if allowlist or denylist intent becomes ambiguous, if the circled control is not owned by `src/App.jsx`, if proof shows the real owner lies outside the allowlist, or if implementing Home-only behavior requires files outside the corrected allowlist.

## Commit Message

`refactor(header): make header control home-only`
