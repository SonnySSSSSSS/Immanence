# TASK-2026-03-27-F: Split PracticeSection.jsx — Extract Config Panel and Session Controls

**Created:** 2026-03-27
**Recommended Executor:** Claude Code / Codex
**Scope:** Extract two self-contained rendering subtrees from `PracticeSection.jsx` into dedicated files. Do not change behavior, state ownership, or prop contracts.
**Flags:** `IMPLEMENT UNCOMMITTED` `NO COMMIT WITHOUT HUMAN APPROVAL`

---

## Goal

`src/components/PracticeSection.jsx` is 3,497 lines. It handles practice configuration UI, session lifecycle, visualization host, audio control, feedback modals, and routing — all in one file. This makes it difficult to navigate, unsafe to edit, and impossible to test individual surfaces in isolation.

This task extracts two bounded rendering subtrees into separate files. The goal is a smaller, more navigable `PracticeSection.jsx` with clean import boundaries — not a full architectural refactor. State ownership, effects, and all business logic stay in `PracticeSection.jsx`.

---

## Problem Statement

### Current structure (approximate line ranges)

| Block | Lines | What it renders |
|-------|-------|-----------------|
| Constants, helpers, DevOverlay | 85–327 | Enums, `PracticeSelector`, `DevCompleteNowOverlay` |
| Component declaration + store hooks | 329–630 | `export function PracticeSection(...)`, all Zustand selectors |
| State declarations | 630–900 | `useState`, `useRef`, `useCallback` declarations |
| Effect hooks | 900–2000 | Session lifecycle, guidance audio, circuit management, keyboard shortcuts |
| Config callbacks & updaters | 2000–2650 | `handleStart`, `handleStop`, config setters |
| **Visualization host / breath session** | **2673–3025** | Breath viewport, canvas layers, session controls — renders when session is active |
| **Config panel (Options Card)** | **3025–3200** | Practice picker, options card, instruction video — renders when session is NOT active |
| Portals / modals | 3200–3350 | `GuidanceAudioController`, `BreathBenchmark`, overlays |
| Footer / submode button array | 3350–3497 | Submode buttons, export |

The two largest distinct rendering subtrees are **Visualization Host** (session active) and **Config Panel** (session inactive). These are ideal extraction targets because:
1. They are conditionally rendered based on `isSessionActive` — they never co-render
2. They receive their data as props (or will after extraction)
3. They contain no side effects (effects stay in parent)

---

## Pre-Implementation Read Gate (Mandatory)

Before writing any code, read the following ranges in `src/components/PracticeSection.jsx`:

1. **Lines 2650–2680** — Find the exact line where the visualization host / breath session block begins (look for the outermost `{isSessionActive && (` or equivalent conditional)
2. **Lines 3020–3060** — Find where the visualization host ends and the config panel begins
3. **Lines 3180–3230** — Find where the config panel (options card) ends
4. **Lines 3350–3420** — Find the submode button array render block

Document the exact line numbers and the prop/variable names used in each block before starting the extraction.

---

## Implementation

### Step 1: Extract `PracticeVisualizationHost`

Create `src/components/practice/PracticeVisualizationHost.jsx`.

This component renders the breath session viewport — the visual canvas, ring overlay, session controls, circuit indicator — everything shown while a session is **active**.

**Extraction approach:**
1. Copy the visualization host JSX block from `PracticeSection.jsx` into the new file
2. Identify every variable and callback referenced in that JSX block
3. Add them all as explicit props to `PracticeVisualizationHost`
4. Back in `PracticeSection.jsx`, replace the inlined JSX with `<PracticeVisualizationHost ...props />`

**Props will likely include** (verify by reading — do not guess):
- `breathState`, `phase`, `progress`
- `practiceId`, `activePracticeConfig`
- `isSessionPaused`, `onPause`, `onResume`, `onStop`
- `ringPreset`, `colorScheme`, `isLight`
- Visualization-specific callbacks and display state

**Component file structure:**
```jsx
// src/components/practice/PracticeVisualizationHost.jsx
export function PracticeVisualizationHost({ /* props */ }) {
  return (
    // extracted JSX here
  );
}
```

### Step 2: Extract `PracticeConfigPanel`

Create `src/components/practice/PracticeConfigPanel.jsx`.

This component renders the configuration UI — the practice picker dropdown, options card, instruction video panel — everything shown while a session is **not yet active**.

**Extraction approach:**
1. Copy the config panel JSX block (options card + practice header) from `PracticeSection.jsx`
2. Identify every variable and callback referenced in that JSX block
3. Add them all as explicit props to `PracticeConfigPanel`
4. Back in `PracticeSection.jsx`, replace the inlined JSX with `<PracticeConfigPanel ...props />`

**Props will likely include** (verify by reading):
- `practiceId`, `practiceList`, `onPracticeSelect`
- `practiceParams`, `onParamChange`
- `duration`, `onDurationChange`
- `onStart`, `colorScheme`, `isLight`
- Config-specific display state

**Component file structure:**
```jsx
// src/components/practice/PracticeConfigPanel.jsx
export function PracticeConfigPanel({ /* props */ }) {
  return (
    // extracted JSX here
  );
}
```

### Step 3: Create the directory

Create `src/components/practice/` if it does not exist. Both new files go here.

### Step 4: Update imports in `PracticeSection.jsx`

Add import statements for both new components at the top of `PracticeSection.jsx`.

---

## What Does NOT Move

The following stay in `PracticeSection.jsx` and are not extracted:

- All `useState`, `useRef`, `useCallback`, `useMemo` declarations
- All `useEffect` hooks (session lifecycle, audio, circuit, keyboard)
- All event handler functions (`handleStart`, `handleStop`, config setters)
- All Zustand store selectors
- `DevCompleteNowOverlay` (dev-only, small)
- `PracticeSelector` helper (small, used only once)
- Portal/modal components (`GuidanceAudioController`, `BreathBenchmark`)

---

## ALLOWLIST (Files to Modify or Create)

1. `src/components/PracticeSection.jsx` — remove extracted JSX, add imports
2. `src/components/practice/PracticeVisualizationHost.jsx` — **create new**
3. `src/components/practice/PracticeConfigPanel.jsx` — **create new**

---

## DENYLIST (Files NOT to Modify)

1. Any state store under `src/state/`
2. `src/App.jsx`
3. Any other component that imports `PracticeSection` — the external API must not change
4. Any CSS file
5. Any file not in the ALLOWLIST

---

## Constraints

1. **Zero behavior change.** The app must behave identically before and after. Session start, stop, pause, resume, config changes — all unchanged.
2. **No state moves.** All state, effects, and business logic stay in `PracticeSection.jsx`.
3. **Explicit props only.** The extracted components receive all data as props — no new Zustand selectors inside them, no new context reads.
4. **No new abstractions.** Extract what is there; do not redesign the prop interface.
5. **The external export `PracticeSection` is unchanged** — same name, same props, same behavior.

---

## Verification

### Static
```bash
npm run lint   # 0 errors
npm run build  # succeeds
```

### Manual

- [ ] Navigate to Practice section — config panel renders correctly
- [ ] Select a practice, configure options — all config controls work
- [ ] Start a session — visualization host renders correctly
- [ ] Session progress, phase transitions, ring animations work
- [ ] Stop/pause/resume session — controls work
- [ ] Navigate away and back — no stale state

### Size check

After extraction, `PracticeSection.jsx` should be under 2,800 lines. If it is not, report remaining line count and largest remaining blocks.

---

## Commit Message

```
refactor(practice): extract PracticeVisualizationHost and PracticeConfigPanel from PracticeSection
```

---

## Stop Gate

`NO COMMIT WITHOUT HUMAN APPROVAL`.

Stop and report if:
- The visualization host and config panel do not have a clean conditional boundary in the JSX (e.g., they are interleaved rather than sequential blocks)
- Any extracted block references a variable that is defined inside another extracted block (circular dependency)
- The resulting `PracticeSection.jsx` is still above 2,800 lines after both extractions
