# TASK-2026-03-26-B: Dead Code and Unused Dependency Removal

**Created:** 2026-03-26
**Recommended Executor:** Claude Code / Codex
**Scope:** Three independent zero-risk deletions — unused npm dependency, temporary debug scripts, and quarantined legacy source directories.
**Flags:** `NO COMMIT WITHOUT HUMAN APPROVAL`

---

## Goal

Remove 12 dead files and 1 unused npm dependency that contribute no runtime value and increase repo noise. All three sub-fixes are mechanical deletions with no impact on the production build or any live code path.

---

## Problem Statement

### B1 — Unused `express` dependency (`package.json:27`)

`express` is listed as a production dependency (`"express": "^4.18.0"`) but is never imported or referenced anywhere in the source tree (`src/`, `scripts/`, `vite.config.js`, or any config file). It adds ~14 MB to `node_modules` and ~14 s to cold CI installs.

**Fix:** Remove the `"express"` line from `dependencies` in `package.json`. Run `npm install` to update `package-lock.json`.

---

### B2 — Temporary debug scripts (`scripts/_tmp_*.mjs`)

Seven scripts prefixed `_tmp_` are leftover from past debugging sessions. None are referenced in `package.json` scripts, any CI workflow, or any import. They are not part of the build.

Files to delete:
- `scripts/_tmp_take_fx_screens2.mjs`
- `scripts/_tmp_verify_picker.mjs`
- `scripts/_tmp_verify_picker2.mjs`
- `scripts/_tmp_verify_picker3.mjs`
- `scripts/_tmp_debug_practice.mjs`
- `scripts/_tmp_debug_practice2.mjs`
- `scripts/_tmp_final_screens.mjs`

**Fix:** Delete all seven files.

---

### B3 — Quarantine directories (`src/__quarantine__graveyard/`, `src/__quarantine__legacy/`)

Two directories hold legacy code that has been superseded:
- `src/__quarantine__graveyard/avatarV1/` — 10 files, ~108 KB, pre-v3 avatar implementation
- `src/__quarantine__legacy/` — 3 files, ~100 KB, graveyard and repair variants of `PracticeSection`

Verified non-imported: a global grep for any import from either directory returns zero results. The git history already preserves this code; keeping it in the working tree adds IDE indexing noise and creates accidental-import risk.

**Fix:** Delete both directories in full.

---

## Spec Lint Gate (Mandatory)

- Single bounded change per sub-fix: yes — each sub-fix is a deletion only
- Exact file allowlist: yes
- No-sequencing rule stated: yes
- Clear screenshot pass/fail signal: N/A — deletion task
- Explicit stop gate: yes

---

## ALLOWLIST (Files/Dirs to Delete or Modify)

**Modify:**
1. `package.json` *(remove express entry)*
2. `package-lock.json` *(updated automatically by npm install)*

**Delete:**
3. `scripts/_tmp_take_fx_screens2.mjs`
4. `scripts/_tmp_verify_picker.mjs`
5. `scripts/_tmp_verify_picker2.mjs`
6. `scripts/_tmp_verify_picker3.mjs`
7. `scripts/_tmp_debug_practice.mjs`
8. `scripts/_tmp_debug_practice2.mjs`
9. `scripts/_tmp_final_screens.mjs`
10. `src/__quarantine__graveyard/` *(entire directory)*
11. `src/__quarantine__legacy/` *(entire directory)*

---

## DENYLIST (Files NOT to Modify)

1. `src/App.jsx`
2. `vite.config.js`
3. Any file in `src/` not listed above
4. Any file in `scripts/` not listed above
5. Any file not listed in the ALLOWLIST

---

## Constraints

1. Do not change any imported source file — only delete files that have been verified as non-imported.
2. For B1: remove only the `express` line. Do not reformat or reorder other `package.json` entries.
3. For B1: run `npm install` after editing `package.json` to sync `package-lock.json`. Do not manually edit the lockfile.
4. For B3: before deleting, confirm with a grep that neither `__quarantine__graveyard` nor `__quarantine__legacy` appears in any import statement in `src/`. If any import is found, stop and report it — do not delete.
5. **No-sequencing rule:** the three sub-fixes are independent. B1 and B2 and B3 may be performed in any order.

---

## Verification

### Pre-deletion import check (mandatory before B3)
```bash
grep -r "__quarantine__" src/ --include="*.jsx" --include="*.js" --include="*.ts" --include="*.tsx"
```
Must return zero results. If any results are found, stop and report before deleting.

### Build
```bash
npm run build
```
Must succeed with no errors. Bundle size for `index-*.js` should remain within ±5% of the pre-task baseline (4,671 kB uncompressed) — the quarantine dirs are not imported so bundle size should be unchanged.

### Lint
```bash
npm run lint
```
Must pass (or show the same pre-existing errors as before this task — this task is not responsible for fixing lint errors).

### Dev server smoke check
```bash
npm run dev
```
App must start and load normally. Navigate to Practice section, verify no runtime errors.

### Screenshot Success/Fail Signal
N/A — deletion task. Pass signal is build success + zero runtime errors.

---

## Commit Message

```
chore(cleanup): remove express dep, temp debug scripts, and quarantine source dirs
```

---

## Stop Gate

`NO COMMIT WITHOUT HUMAN APPROVAL`.

If the pre-deletion grep in B3 finds any import from a quarantine directory, stop immediately — do not delete those files. Report the finding and wait for direction.
