# TASK-2026-03-14-A: Audit Student Signup -> Initiation -> Practice Completion Readiness

**Created:** 2026-03-14
**Recommended Executor:** Codex
**Scope:** One end-to-end readiness audit for the first-time student flow
**Flags:** `IMPLEMENT UNCOMMITTED, VERIFY FIRST` `NO COMMIT WITHOUT HUMAN APPROVAL`

---

## Goal

Audit whether a brand-new student can successfully complete this exact flow without code changes:

`sign up -> choose student mode -> select Initiation path -> begin required practice -> complete practice -> see correct persisted completion state`

Write the audit findings only.

---

## Problem Statement

The repo has critical-flow docs and smoke coverage, but we do not yet have one explicit readiness verdict for the exact first-time student onboarding path that real students would use.

---

## Single Hypothesis

If this flow is not production-usable, the failure will be isolated to one concrete runtime boundary in auth, user-mode selection, path activation, practice launch, completion handling, or persistence rehydration.

---

## Single Atomic Change

Perform one audit and record findings in a single task report.
Do not fix code, refactor, or bundle follow-up changes.

---

## Reuse-First Plan (Mandatory)

1. Reuse existing flow map in `docs/CRITICAL_FLOWS.md`.
2. Reuse existing smoke coverage in `tests/smoke/critical-flows.spec.ts`.
3. Reuse existing runtime surfaces only for inspection:
   - `src/components/auth/AuthGate.jsx`
   - `src/App.jsx`
   - `src/components/HomeHub.jsx`
   - `src/components/NavigationSection.jsx`
   - `src/components/PathOverviewPanel.jsx`
   - `src/components/PracticeSection.jsx`
   - `src/components/practice/SessionSummaryModal.jsx`
4. Reuse existing state/service sources of truth only for inspection:
   - `src/state/useAuthUser.js`
   - `src/state/userModeStore.js`
   - `src/state/navigationStore.js`
   - `src/state/curriculumStore.js`
   - `src/state/uiStore.js`
   - `src/services/sessionRecorder.js`

No new component, store, service, helper, or test in this task.

---

## Spec Lint Gate (Mandatory)

- Single hypothesis: yes
- Single atomic change: yes
- Exact file allowlist: yes
- Explicit no-sequencing rule: yes
- Clear screenshot pass/fail signal: yes
- Explicit stop gate: yes

If any item becomes ambiguous during execution, stop and request a corrected spec.

---

## Stateful UI Debug Gate (Mandatory)

1. **Exact render surfaces**
- Auth: `src/components/auth/AuthGate.jsx`
- User mode chooser: `src/App.jsx`
- Home hub: `src/components/HomeHub.jsx`
- Navigation path wizard: `src/components/NavigationSection.jsx`
- Path onboarding overlay: `src/components/PathOverviewPanel.jsx`
- Practice runtime: `src/components/PracticeSection.jsx`
- Completion modal: `src/components/practice/SessionSummaryModal.jsx`

2. **Exact upstream state sources**
- Auth session: Supabase session via `src/state/useAuthUser.js`
- User mode truth: `src/state/userModeStore.js`
- Active path truth: `src/state/navigationStore.js`
- Curriculum/day-leg truth: `src/state/curriculumStore.js`
- Launch-context truth: `src/state/uiStore.js`
- Completion recording truth: `src/services/sessionRecorder.js`

3. **Concrete runtime cases that must be proved**
- Fresh signed-out boot shows auth gate
- Freshly authenticated user can reach and persist `student` mode
- Initiation path requires valid slot selection before begin
- Practice can be launched from the student flow
- Practice completion shows confirmation UI
- Reload preserves the expected active/completed state without stray overlays

4. **Visual vs logic isolation**
- It is allowed to use the existing fake-session smoke path only to isolate post-auth runtime from live auth issues
- Final readiness verdict must still separately label real signup/auth as `PASS`, `FAIL`, or `UNVERIFIED`

5. **Exact audit predicate**
- Mark the flow `READY` only if every stage above has runtime proof and no blocker remains at severity P0 or P1

If any proof is missing, stop and do not implement fixes.

---

## ALLOWLIST (Files to Modify)

1. `docs/TASK-2026-03-14-A.md`

---

## DENYLIST (Files NOT to Modify)

1. `src/components/auth/AuthGate.jsx`
2. `src/App.jsx`
3. `src/components/HomeHub.jsx`
4. `src/components/NavigationSection.jsx`
5. `src/components/PathOverviewPanel.jsx`
6. `src/components/PracticeSection.jsx`
7. `src/components/practice/SessionSummaryModal.jsx`
8. `src/state/useAuthUser.js`
9. `src/state/userModeStore.js`
10. `src/state/navigationStore.js`
11. `src/state/curriculumStore.js`
12. `src/state/uiStore.js`
13. `src/services/sessionRecorder.js`
14. `tests/smoke/critical-flows.spec.ts`

---

## Constraints

1. Audit only; no runtime code changes.
2. Do not edit any file outside ALLOWLIST.
3. No "while I'm here" fixes, cleanup, or refactors.
4. No schema, persistence, or Supabase changes.
5. Use exact before/after runtime evidence for every failed stage.
6. If signup depends on live email confirmation or environment credentials, label that stage explicitly as `UNVERIFIED` rather than guessing.
7. **No-sequencing rule:** this task is one readiness audit only; do not combine it with remediation.

---

## Verification

### Automated

1. Run existing smoke coverage relevant to the post-auth flow:
- `npx playwright test tests/smoke/critical-flows.spec.ts`

2. Run existing path/state regression coverage:
- `node --test test/pathContract.test.js`
- `node --test test/pathActivationGuards.test.js`
- `node --test test/initiationBenchmarkAttempt.test.js`

### Manual Runtime

1. Start app from canonical workspace:
- `npm run dev`

2. Real first-time flow audit:
- Boot signed out and confirm auth gate
- Attempt real signup with a fresh throwaway email
- Confirm post-auth transition
- Select `student` mode
- Open `Navigation`
- Select `Paths`
- Open `Initiation`
- Prove invalid slot case is blocked
- Select exactly 2 slots and begin path
- Launch the required practice
- Complete the practice
- Confirm `SESSION COMPLETE`
- Reload and confirm persisted state is still correct

3. Required audit output in the report
- Stage-by-stage table: `PASS | FAIL | UNVERIFIED`
- First blocking node with exact file/component boundary
- One concrete failing case for each failed stage
- Final verdict: `READY`, `NOT READY`, or `PARTIALLY VERIFIED`

### Screenshot Success/Fail Signal (Mandatory)

1. **Fail signal:** any stage stalls, crashes, loops back incorrectly, or completes without the expected visible surface
2. **Pass signal sequence:** auth gate visible -> student mode chooser visible -> initiation slot step visible -> active path visible -> `SESSION COMPLETE` visible -> correct state still visible after reload

---

## Commit Message

`docs(audit): record first-time student signup and initiation readiness`

---

## Stop Gate (Mandatory)

After the audit report is written, stop.
Do not fix anything and do not commit until explicit human approval is given.
